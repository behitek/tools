name: Deploy Tools to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build and deploy job
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: './gross-net-convert/package.json'

      - name: Install dependencies for gross-net-convert
        run: |
          cd gross-net-convert
          npm ci

      - name: Build and export gross-net-convert
        run: |
          cd gross-net-convert
          npm run build

      - name: Create deployment directory structure
        run: |
          mkdir -p ./deploy-site
          mkdir -p ./deploy-site/emoji-picker
          mkdir -p ./deploy-site/salary-calculator

      - name: Copy emoji-picker files
        run: |
          cp -r ./emoji-picker/* ./deploy-site/emoji-picker/

      - name: Copy gross-net-convert build files
        run: |
          cp -r ./gross-net-convert/out/* ./deploy-site/salary-calculator/

      - name: Create main index.html
        run: |
          cat > ./deploy-site/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="vi">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>🛠️ Bộ công cụ tiện ích - Luyencode Tools</title>
              <meta name="description" content="Bộ sưu tập các công cụ tiện ích miễn phí: Máy tính lương, Bộ chọn emoji và nhiều công cụ khác">
              <style>
                  * { margin: 0; padding: 0; box-sizing: border-box; }
                  body { 
                      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
                      line-height: 1.6; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                      min-height: 100vh; display: flex; align-items: center; justify-content: center;
                  }
                  .container { 
                      max-width: 800px; margin: 0 auto; padding: 2rem; 
                      background: rgba(255, 255, 255, 0.95); border-radius: 20px;
                      box-shadow: 0 20px 40px rgba(0,0,0,0.1); backdrop-filter: blur(10px);
                  }
                  h1 { 
                      text-align: center; color: #333; margin-bottom: 2rem; font-size: 2.5rem;
                      background: linear-gradient(135deg, #667eea, #764ba2); -webkit-background-clip: text;
                      -webkit-text-fill-color: transparent; background-clip: text;
                  }
                  .tools-grid { 
                      display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                      gap: 2rem; margin-top: 2rem;
                  }
                  .tool-card { 
                      background: white; border-radius: 15px; padding: 2rem; text-decoration: none;
                      box-shadow: 0 10px 25px rgba(0,0,0,0.1); transition: all 0.3s ease;
                      border: 2px solid transparent;
                  }
                  .tool-card:hover { 
                      transform: translateY(-5px); box-shadow: 0 20px 40px rgba(0,0,0,0.15);
                      border-color: #667eea;
                  }
                  .tool-icon { font-size: 3rem; margin-bottom: 1rem; }
                  .tool-title { 
                      color: #333; font-size: 1.5rem; font-weight: 600; margin-bottom: 0.5rem;
                  }
                  .tool-description { 
                      color: #666; font-size: 1rem; line-height: 1.5;
                  }
                  .footer {
                      text-align: center; margin-top: 3rem; color: #666; font-size: 0.9rem;
                  }
                  .footer a { color: #667eea; text-decoration: none; }
                  .footer a:hover { text-decoration: underline; }
              </style>
          </head>
          <body>
              <div class="container">
                  <h1>🛠️ Bộ công cụ tiện ích</h1>
                  <p style="text-align: center; color: #666; font-size: 1.1rem; margin-bottom: 2rem;">
                      Tổng hợp các công cụ hữu ích được phát triển bởi Luyencode
                  </p>
                  
                  <div class="tools-grid">
                      <a href="./salary-calculator/" class="tool-card">
                          <div class="tool-icon">💰</div>
                          <h3 class="tool-title">Máy tính lương</h3>
                          <p class="tool-description">
                              Tính toán chính xác lương gross-net theo luật Việt Nam 2025. 
                              Hỗ trợ tùy chỉnh vùng, mức đóng bảo hiểm linh hoạt.
                          </p>
                      </a>
                      
                      <a href="./emoji-picker/" class="tool-card">
                          <div class="tool-icon">😊</div>
                          <h3 class="tool-title">Bộ chọn Emoji</h3>
                          <p class="tool-description">
                              Tìm kiếm và sao chép emoji nhanh chóng với bộ sưu tập đầy đủ. 
                              Giao diện thân thiện, dễ sử dụng.
                          </p>
                      </a>
                  </div>
                  
                  <div class="footer">
                      <p>
                          Phát triển bởi <a href="https://luyencode.net" target="_blank">Luyencode</a> |
                          <a href="https://github.com/behitek/tools" target="_blank">GitHub</a>
                      </p>
                  </div>
              </div>
          </body>
          </html>
          EOF

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy-site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4